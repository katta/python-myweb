version = "1.0"

subprojects {
    task clean(type: Delete) {
        delete 'dist'
        delete 'wheelhouse'
    }

    task build(type:Exec, dependsOn: clean) {
        commandLine 'python', 'setup.py', 'package'
//        commandLine 'cat', 'readme.txt'
    }
}
