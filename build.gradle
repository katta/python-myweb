buildscript {
    repositories {
        jcenter()
    }
    dependencies{
        classpath 'com.linkedin.pygradle:pygradle-plugin:0.7.18'
    }
}

apply plugin: "com.linkedin.python-sdist"
//apply plugin: com.linkedin.gradle.python.plugin.WheelFirstPlugin

dependencies {
    python 'pypi:Flask:0.12.2'
//    test 'pypi:mock:1.3.0'
}

repositories {
    pyGradlePyPi()
}